# Changelog

## 1.1.0

### Breaking
- Renamed plugin from `codex` to `codex-jr` to resolve short-name collision with upstream `codex@openai-codex`. All slash commands now use the `/codex-jr:` prefix (e.g. `/codex-jr:setup`, `/codex-jr:review`). Update your invocations accordingly.

## 1.0.0

- Initial version of the Codex plugin for Claude Code
